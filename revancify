#!/data/data/com.termux/files/usr/bin/bash

if cd ~/storage/Revancify > /dev/null 2>&1 
then
    clear
    rm -rf ./*cache
    git check . && git pull && bash revancify.sh
else
    echo "Revancify is not installed"
    exit
fi